# P10 demo recording

The v2.2 kit makes the video optional and limits it to three minutes. This outline takes about two minutes. Record the deployed build that matches the submitted commit, then add the public video link to the README and `demo_video_url_optional` in the manifest.

Keep the actions visible and read the current values from the screen. The video must cover the problem-solving method and every member's contribution as well as the working app.

1. 0:00 to 0:15. Start on PUB-01. Say: "KotoDin helps a household check its prepaid meter balance and plan a recharge. This sample has six months of readings, a light January, a heavy May and a large recharge in May's last week." Briefly switch to বাংলা and back to English.
2. 0:15 to 0:30. Use "Load judge JSON" to open `data/P10_prepaid_meter_public.json`, then show "Restore samples". Say: "The app accepts the official file or a same-shape judge case. It validates the dates and amounts before replacing the current data. Restore samples brings back the published cases."
3. 0:30 to 0:48. Show "Daily balance" and move the day slider onto a recharge date. Say: "Every recharge is marked. The selected day shows its use, charges and closing balance. The monthly table separates deposits from costs." On a narrow screen, scroll the chart sideways so its labels stay readable.
4. 0:48 to 1:05. Point to the run-out date, then change the target date. Say: "At the case's usual daily use, the balance reaches zero or below on the date shown here. A new target date updates the required top-up, with energy, higher-slab cost, fixed charges and VAT shown separately."
5. 1:05 to 1:25. Show PUB-01's habit comparison, then select PUB-02 and return to the same section. Say: "Both habits use identical consumption. PUB-01 costs the same under either policy. PUB-02 differs because of fixed charges, not cheaper energy rates. The energy-cost difference stays zero."
6. 1:25 to 1:38. Show the tariff and calculation steps. Say: "One tariff function uses integer paisa for the history, forecasts and comparison. Tests check slab boundaries, first-recharge charges, invalid inputs and the cost totals."
7. 1:38 to 2:00. Finish with the contribution table in the README. Say: "Adib led P10, reviewed the rules and integrated the AI-assisted core application. Saumik created the registered repository and arranged team access. Samiyeel hardened imports and target dates, added regression tests and improved font fallbacks. We check the submitted work with automated tests and the browser checklist."

Before recording, practise the file selection and case switch once. Keep the pointer near the value being discussed. Do not add a video link until the uploaded recording is accessible without signing in.
